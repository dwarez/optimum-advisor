# Release automation (https://release-plz.dev).
#
# On every push to main, CI opens/updates a release PR that bumps the version
# and CHANGELOG.md from the commits since the last tag. Merging that PR makes
# CI create the `v{version}` git tag and the GitHub release; the binary build
# workflow then attaches the prebuilt musl artifact to it.

[workspace]
# Git-only mode: this crate is not on crates.io, so versions are determined
# from git tags; release-plz manages the version bump, changelog, git tag, and
# GitHub release. BOTH keys below are required: `git_only` alone does NOT
# disable `cargo publish` (release-plz requires `publish` to be explicitly
# false when `git_only` is set). Note: Cargo.toml must NOT set
# `publish = false` — the `release` command silently skips unpublishable
# packages, even in git-only mode.
git_only = true
publish = false
# Keep `feat:` commits bumping the minor version also while on 0.x.
features_always_increment_minor = true
# Open/refresh a release PR only when a functional commit landed since the
# last tag. Docs, CI, and tooling commits (`change:`, `doc:`, `chore:`,
# `ci:`, `test:`) do not cut releases by themselves; they ride along in the
# next release triggered by a matching commit. File-based gating is not an
# option: release-plz's git-only change detection bumps on any commit,
# regardless of cargo package contents.
release_commits = "^(feat|fix|add|remove|refactor|perf)[(!:]"
# The tag format is load-bearing: the hf-jobs launcher downloads
# `releases/download/v{CARGO_PKG_VERSION}/...` inside the job.
git_tag_name = "v{{ version }}"
git_release_name = "v{{ version }}"

[changelog]
# Group commits using this repository's message conventions alongside the
# standard conventional-commit types. Version bumps still follow conventional
# commits: `feat:` bumps minor, `fix:` and everything else bump patch, and a
# `!`/BREAKING CHANGE footer bumps major.
commit_parsers = [
    { message = "^feat", group = "Added" },
    { message = "^add", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^change", group = "Changed" },
    { message = "^refactor", group = "Changed" },
    { message = "^remove", group = "Removed" },
    { message = "^doc", group = "Documentation" },
    { message = "^perf", group = "Performance" },
    { message = "^chore|^ci|^test", group = "Miscellaneous" },
    { message = ".*", group = "Other" },
]
